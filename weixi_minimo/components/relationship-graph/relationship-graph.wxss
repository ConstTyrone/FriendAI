/* 关系图谱可视化组件样式 */
.relationship-graph {
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  background: var(--td-bg-color-page);
}

/* 工具栏样式 */
.graph-toolbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx 24rpx;
  background: var(--td-bg-color-container);
  border-bottom: 1rpx solid var(--td-border-level-1-color);
  box-shadow: 0 1rpx 4rpx rgba(0, 0, 0, 0.05);
  position: relative;
  z-index: 10;
}

.toolbar-left,
.toolbar-right {
  display: flex;
  gap: 16rpx;
}

/* 工具栏按钮增强 */
.graph-toolbar t-button {
  border-radius: 8rpx;
  transition: all 0.2s ease;
}

.graph-toolbar t-button:hover {
  transform: translateY(-1rpx);
  box-shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.1);
}

/* 统计信息样式 */
.graph-stats {
  display: flex;
  justify-content: space-around;
  padding: 20rpx 24rpx;
  background: linear-gradient(135deg, var(--td-bg-color-container) 0%, rgba(6, 174, 86, 0.02) 100%);
  border-bottom: 1rpx solid var(--td-border-level-1-color);
  position: relative;
  z-index: 5;
}

.graph-stats::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2rpx;
  background: linear-gradient(90deg, transparent 0%, #06ae56 50%, transparent 100%);
  opacity: 0.6;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6rpx;
  position: relative;
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  transition: all 0.3s ease;
}

.stat-item:hover {
  background: rgba(6, 174, 86, 0.08);
  transform: translateY(-2rpx);
}

.stat-number {
  font-size: 36rpx;
  font-weight: 700;
  color: #06ae56;
  background: linear-gradient(135deg, #06ae56 0%, rgba(6, 174, 86, 0.8) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  line-height: 1.2;
  font-variant-numeric: tabular-nums;
  text-shadow: 0 2rpx 4rpx rgba(6, 174, 86, 0.2);
}

.stat-label {
  font-size: 24rpx;
  color: var(--td-text-color-secondary);
  font-weight: 500;
  letter-spacing: 0.5rpx;
}

/* 画布容器样式 */
.graph-canvas-container {
  flex: 1;
  position: relative;
  overflow: hidden;
}

.graph-canvas {
  width: 100%;
  height: 100%;
  background: var(--td-bg-color-container);
}

/* 加载状态样式 */
.loading-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  background: rgba(255, 255, 255, 0.8);
}

/* 空状态样式 */
.empty-state {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
  color: var(--td-text-color-placeholder);
}

.empty-text {
  font-size: 32rpx;
  font-weight: 500;
}

.empty-tip {
  font-size: 28rpx;
  text-align: center;
  max-width: 400rpx;
}

/* 图例样式 */
.graph-legend {
  padding: 16rpx 24rpx;
  background: var(--td-bg-color-container);
  border-top: 1rpx solid var(--td-border-level-1-color);
  z-index: 20;
  position: relative;
  box-shadow: 0 -2rpx 12rpx rgba(0, 0, 0, 0.08);
  margin-bottom: calc(0rpx + env(safe-area-inset-bottom));
}

.legend-title {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--td-text-color-primary);
  margin-bottom: 16rpx;
  position: relative;
  padding-left: 24rpx;
}

.legend-title::before {
  content: '📊';
  position: absolute;
  left: 0;
  font-size: 20rpx;
}

.legend-content {
  white-space: nowrap;
}

.legend-items {
  display: flex;
  gap: 24rpx;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex-shrink: 0;
  padding: 6rpx 12rpx;
  border-radius: 20rpx;
  background: rgba(0, 0, 0, 0.02);
  transition: all 0.2s ease;
  border: 1rpx solid transparent;
}

.legend-item:hover {
  background: rgba(6, 174, 86, 0.1);
  border-color: rgba(6, 174, 86, 0.2);
  transform: translateY(-1rpx);
}

.legend-color {
  width: 16rpx;
  height: 16rpx;
  border-radius: 8rpx;
  box-shadow: 0 1rpx 3rpx rgba(0, 0, 0, 0.2);
}

.legend-text {
  font-size: 24rpx;
  color: var(--td-text-color-secondary);
  font-weight: 500;
}

/* 交互提示样式 */
.interaction-tips {
  margin-top: 20rpx;
  padding-top: 20rpx;
  border-top: 1rpx solid var(--td-border-level-1-color);
}

.tips-title {
  font-size: 24rpx;
  font-weight: 600;
  color: var(--td-text-color-primary);
  margin-bottom: 12rpx;
}

.tips-list {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
}

.tip-item {
  font-size: 22rpx;
  color: var(--td-text-color-placeholder);
  background: rgba(6, 174, 86, 0.05);
  padding: 4rpx 8rpx;
  border-radius: 12rpx;
  border: 1rpx solid rgba(6, 174, 86, 0.1);
  line-height: 1.2;
  transition: all 0.2s ease;
}

.tip-item:hover {
  background: rgba(6, 174, 86, 0.1);
  border-color: rgba(6, 174, 86, 0.2);
  color: var(--td-text-color-secondary);
}

/* 节点详情弹窗样式 */
.node-detail-popup {
  padding: 32rpx;
  background: var(--td-bg-color-container);
  border-radius: 24rpx 24rpx 0 0;
  max-width: 100vw;
  overflow: hidden;
}

.node-detail-header {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 24rpx;
  width: 100%;
  min-width: 0;
}

.node-detail-info {
  flex: 1;
  min-width: 0;
  overflow: hidden;
}

.node-name {
  font-size: 36rpx;
  font-weight: 600;
  color: var(--td-text-color-primary);
  margin-bottom: 8rpx;
  word-break: break-all;
  overflow-wrap: break-word;
}

.node-company {
  font-size: 28rpx;
  color: var(--td-text-color-secondary);
  margin-bottom: 4rpx;
  word-break: break-all;
  overflow-wrap: break-word;
}

.node-position {
  font-size: 26rpx;
  color: var(--td-text-color-placeholder);
  word-break: break-all;
  overflow-wrap: break-word;
}

.node-detail-stats {
  display: flex;
  justify-content: space-around;
  padding: 24rpx 0;
  margin: 24rpx 0;
  background: var(--td-bg-color-page);
  border-radius: 12rpx;
}

.detail-stat {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.node-detail-tags {
  margin: 24rpx 0;
}

.tags-title {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--td-text-color-primary);
  margin-bottom: 16rpx;
}

.tags-list {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.node-detail-actions {
  display: flex;
  gap: 16rpx;
  justify-content: flex-end;
  margin-top: 32rpx;
}

/* 关系详情弹窗样式 */
.link-detail-popup {
  padding: 32rpx;
  background: var(--td-bg-color-container);
  border-radius: 24rpx 24rpx 0 0;
  max-width: 100vw;
  overflow: hidden;
}

.link-detail-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
  width: 100%;
  min-width: 0;
}

.link-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--td-text-color-primary);
  flex: 1;
  margin-right: 16rpx;
  min-width: 0;
  word-break: break-all;
  overflow-wrap: break-word;
  white-space: pre-wrap;
}

.link-detail-content {
  margin: 24rpx 0;
}

.detail-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid var(--td-border-level-1-color);
}

.detail-row:last-child {
  border-bottom: none;
}

.detail-label {
  font-size: 28rpx;
  color: var(--td-text-color-secondary);
}

.detail-value {
  font-size: 28rpx;
  color: var(--td-text-color-primary);
  text-align: right;
  flex: 1;
  margin-left: 24rpx;
  min-width: 0;
  word-break: break-all;
  overflow-wrap: break-word;
}

.link-detail-actions {
  display: flex;
  gap: 16rpx;
  justify-content: flex-end;
  margin-top: 32rpx;
}

/* 设置弹窗样式 */
.settings-popup {
  padding: 32rpx;
  background: var(--td-bg-color-container);
  border-radius: 24rpx 24rpx 0 0;
  max-height: 70vh;
  max-width: 100vw;
  overflow: hidden;
}

.settings-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32rpx;
  width: 100%;
  min-width: 0;
}

.settings-title {
  font-size: 36rpx;
  font-weight: 600;
  color: var(--td-text-color-primary);
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.settings-content {
  max-height: 60vh;
  overflow-y: auto;
}

.setting-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx 0;
  border-bottom: 1rpx solid var(--td-border-level-1-color);
  width: 100%;
  min-width: 0;
  gap: 16rpx;
}

.setting-item:last-child {
  border-bottom: none;
}

.setting-label {
  font-size: 30rpx;
  color: var(--td-text-color-primary);
  flex: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.setting-value {
  font-size: 26rpx;
  color: var(--td-text-color-secondary);
  margin-left: 16rpx;
  flex-shrink: 0;
  white-space: nowrap;
}

/* 响应式设计 */
@media (max-width: 400px) {
  .graph-stats {
    padding: 12rpx 16rpx;
  }
  
  .stat-number {
    font-size: 28rpx;
  }
  
  .stat-label {
    font-size: 22rpx;
  }
  
  .toolbar-left,
  .toolbar-right {
    gap: 8rpx;
  }
}